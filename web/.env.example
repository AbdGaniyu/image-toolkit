# Base URL of the image API (api/). Exposed to the browser, so it's read at
# build time: set it before `npm run build`, including on Vercel. Production
# builds require https://; http://localhost is fine for `npm run dev`.
NEXT_PUBLIC_API_URL=http://localhost:8000
